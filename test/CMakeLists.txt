#enable_language(Fortran)
enable_language (Fortran)
##set (PFUNIT /home/jonny/contrib-gcc540/pFUnit-3.2.8-ser)
# set (PFUNIT /home/tml/src/pFUnit)
# set (PFUNIT ${PFUNIT_INSTALL})
set (CMAKE_VERBOSE_MAKEFILE "On")

find_package(PFUNIT REQUIRED)
enable_testing()

include_directories(
  ${CMAKE_CURRENT_SOURCE_DIR}
  ${CMAKE_CURRENT_SOURCE_DIR}/../puffin
  ${CMAKE_CURRENT_BINARY_DIR}
  ${CMAKE_CURRENT_BINARY_DIR}/../puffin
  )
  

link_directories(
  ${Fftw3_LIBRARY_DIRS}
)

# Define tests to build:

#set (PFTESTS testTests testFFunctions testHdf5Puff)

# add_pfunit_ctest (puffin_tests
#   TEST_SOURCES testTests.pf testFFunctions.pf testHdf5Puff.pf # your application library
# )

# Copy test input files to build directory so tests can access them
configure_file(
  "${CMAKE_CURRENT_SOURCE_DIR}/inputs/f1main.in"
  "${CMAKE_CURRENT_BINARY_DIR}/inputs/f1main.in"
  COPYONLY
)

configure_file(
  "${CMAKE_CURRENT_SOURCE_DIR}/inputs/beam_file.in"
  "${CMAKE_CURRENT_BINARY_DIR}/inputs/beam_file.in"
  COPYONLY
)

configure_file(
  "${CMAKE_CURRENT_SOURCE_DIR}/inputs/seed_file.in"
  "${CMAKE_CURRENT_BINARY_DIR}/inputs/seed_file.in"
  COPYONLY
)

configure_file(
  "${CMAKE_CURRENT_SOURCE_DIR}/expected/f1main_expected_field.h5"
  "${CMAKE_CURRENT_BINARY_DIR}/expected/f1main_expected_field.h5"
  COPYONLY
)

configure_file(
  "${CMAKE_CURRENT_SOURCE_DIR}/expected/f1main_expected_electrons.h5"
  "${CMAKE_CURRENT_BINARY_DIR}/expected/f1main_expected_electrons.h5"
  COPYONLY
)

configure_file(
  "${CMAKE_CURRENT_SOURCE_DIR}/expected/f1main_expected_integrated.h5"
  "${CMAKE_CURRENT_BINARY_DIR}/expected/f1main_expected_integrated.h5"
  COPYONLY
)

# 3D CLARA single-slice test inputs
configure_file(
  "${CMAKE_CURRENT_SOURCE_DIR}/inputs/3D/clara_test.in"
  "${CMAKE_CURRENT_BINARY_DIR}/inputs/3D/clara_test.in"
  COPYONLY
)

configure_file(
  "${CMAKE_CURRENT_SOURCE_DIR}/inputs/3D/beam_file_h5.in"
  "${CMAKE_CURRENT_BINARY_DIR}/inputs/3D/beam_file_h5.in"
  COPYONLY
)

configure_file(
  "${CMAKE_CURRENT_SOURCE_DIR}/inputs/3D/seed_file.in"
  "${CMAKE_CURRENT_BINARY_DIR}/inputs/3D/seed_file.in"
  COPYONLY
)

# HDF5 files generated by the one-off seed/golden runs — guarded so CMake can
# configure before they exist.  Run the seed and golden runs to populate them,
# then re-run cmake to pick them up.
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/inputs/3D/clara_electrons_0.h5")
  configure_file(
    "${CMAKE_CURRENT_SOURCE_DIR}/inputs/3D/clara_electrons_0.h5"
    "${CMAKE_CURRENT_BINARY_DIR}/inputs/3D/clara_electrons_0.h5"
    COPYONLY
  )
endif()

if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_expected_field.h5")
  configure_file(
    "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_expected_field.h5"
    "${CMAKE_CURRENT_BINARY_DIR}/expected/3D/clara_expected_field.h5"
    COPYONLY
  )
endif()

if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_expected_electrons.h5")
  configure_file(
    "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_expected_electrons.h5"
    "${CMAKE_CURRENT_BINARY_DIR}/expected/3D/clara_expected_electrons.h5"
    COPYONLY
  )
endif()

if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_expected_integrated.h5")
  configure_file(
    "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_expected_integrated.h5"
    "${CMAKE_CURRENT_BINARY_DIR}/expected/3D/clara_expected_integrated.h5"
    COPYONLY
  )
endif()

add_pfunit_ctest (puffin_basic_tests
  TEST_SOURCES testTests.pf testFFunctions.pf testLatticeElements.pf
  LINK_LIBRARIES puffin_mpi
)


add_pfunit_ctest (puffin_e2e_tests
  TEST_SOURCES testMPIIntegration.pf
  LINK_LIBRARIES puffin_mpi
  MAX_PES 2
)

# 3D test gets its own binary so puffin's global allocatables are clean
add_pfunit_ctest (puffin_e2e_tests_3d
  TEST_SOURCES testMPIIntegration3D.pf
  LINK_LIBRARIES puffin_mpi
  MAX_PES 2
)



# Slow full-lattice 3D test — gated by CMake option, not run by default
option(PUFFIN_SLOW_TESTS "Build slow integration tests (full CLARA 17-module lattice, takes minutes)" OFF)

if(PUFFIN_SLOW_TESTS)

  configure_file(
    "${CMAKE_CURRENT_SOURCE_DIR}/inputs/3D/clara_full.in"
    "${CMAKE_CURRENT_BINARY_DIR}/inputs/3D/clara_full.in"
    COPYONLY
  )

  configure_file(
    "${CMAKE_CURRENT_SOURCE_DIR}/inputs/3D/clara.latt"
    "${CMAKE_CURRENT_BINARY_DIR}/inputs/3D/clara.latt"
    COPYONLY
  )

  if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_full_expected_field.h5")
    configure_file(
      "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_full_expected_field.h5"
      "${CMAKE_CURRENT_BINARY_DIR}/expected/3D/clara_full_expected_field.h5"
      COPYONLY
    )
  endif()

  if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_full_expected_electrons.h5")
    configure_file(
      "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_full_expected_electrons.h5"
      "${CMAKE_CURRENT_BINARY_DIR}/expected/3D/clara_full_expected_electrons.h5"
      COPYONLY
    )
  endif()

  if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_full_expected_integrated.h5")
    configure_file(
      "${CMAKE_CURRENT_SOURCE_DIR}/expected/3D/clara_full_expected_integrated.h5"
      "${CMAKE_CURRENT_BINARY_DIR}/expected/3D/clara_full_expected_integrated.h5"
      COPYONLY
    )
  endif()

  add_pfunit_ctest(puffin_e2e_tests_3d_slow
    TEST_SOURCES testMPIIntegration3DSlow.pf
    LINK_LIBRARIES puffin_mpi
    MAX_PES 2
  )

endif()

# ^^ For examples of adding more tests, see:
# https://github.com/Goddard-Fortran-Ecosystem/pFUnit_demos/blob/main/Basic/tests/CMakeLists.txt
